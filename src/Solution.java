import java.util.List;

class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> triplets = new List<List<Integer>>;

        for (int i = 1; i < nums.length - 1; i++) {
            if (nums[0] != nums[i] &&
                    nums[0] != nums[i + 1] &&
                    nums[i] != nums[i + 1]) {
                if (nums[0] + nums[i] + nums[i + 1] = 0) {
                    //check for uniqueness with triplets
                }
                }
            }
        }
    }
}